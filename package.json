{
  "name": "setclick",
  "private": true,
  "description": "Dev tooling for SetClick. The app itself is metronome.html and has no build step.",
  "scripts": {
    "test": "playwright test",
    "screenshots": "node scripts/screenshots.js"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
